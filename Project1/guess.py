# pick a word to guess and store it in a variable
word = "Success"
guess = ""
# start a while loop which ends when user's input matches the previously defined word
while word != guess:
    # Take a user input
    guess = input("Enter your guess: ")
